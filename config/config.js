/*
 * Do not forget to edit the port if necessary.
 * Default is 8000 (server-side AND client-side).
 * Then, do not forget to edit the file /static/js/functions.js
 * on the firsts lines to set the hosts informations (IP, domain name
 * and port).
 */
PORT = 8000;